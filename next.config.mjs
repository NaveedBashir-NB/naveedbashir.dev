/** @type {import('next').NextConfig} */ 
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  // basePath: "/naveedbashir.dev", // GitHub repo name
  // assetPrefix: "/naveedbashir.dev",
};

export default nextConfig;
